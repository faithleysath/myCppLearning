enum bits {one = 1, two = 2, four = 4, eight = 8}; // 指定的值必须是整数
enum bigstep {first, second = 100, third}; // first是0，third是101
enum {zero, null = 0, one, numero_nuo = 1}; // zero和null都是0，one和numero_uno都是1